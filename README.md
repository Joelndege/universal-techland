# TouristAlertSystem - Django App for Tourist Safety

A Django-based web application for tourist safety alerts, incident reporting, and community forums. This app is designed to run on Render with no GIS dependencies.

## Local Development Setup

1. Clone the repository
2. Create virtual environment:
   ```bash
   python -m venv venv
   # On Windows:
   venv\Scripts\activate
   # On Linux/Mac:
   source venv/bin/activate
